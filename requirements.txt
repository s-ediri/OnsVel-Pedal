# Runtime dependencies for OnsVel-Pedal.
#
# This file intentionally contains Python packages only. The old
# assets/requirements.txt was a Linux conda environment export with platform
# libraries such as libgcc, qt, sqlite, zlib, etc.; those are not portable pip
# requirements and should be managed by conda itself when needed.
#
# Recommended Windows Conda environment:
#   conda env create -f environment.yml
#   conda activate onsvel
#   python -m pytest tests -q

coloredlogs>=15.0
flask>=2.0
h5py>=3.6
matplotlib>=3.7
mido>=1.2
mir-eval>=0.7
numpy>=1.23,<2.0
omegaconf>=2.1
pandas>=1.4
parse>=1.19
pillow>=9.0
psutil>=5.9
pytz>=2022.0
pyyaml>=6.0
scipy>=1.10
torch>=1.11
torchaudio>=0.11
unidecode>=1.3
werkzeug>=2.0
pydub>=0.25
# Python 3.13 removed the stdlib audioop module used by pydub.
audioop-lts>=0.2.2; python_version >= "3.13"

# Test/development support
pytest>=7.0
